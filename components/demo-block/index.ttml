<view class="custom-class demo-block van-clearfix {{ padding ? 'demo-block--padding' : '' }}">
  <view tt:if="{{ title }}" class="demo-block__title">{{ title }}</view>
  <view class="demo-block__card" tt:if="{{ card }}">
    <slot>
  </slot></view>
 <slot tt:else>
</slot></view>
