<sjs src="./index.sjs" module="computed"></sjs>

<view class="van-grid custom-class {{ border && !gutter ? 'van-hairline--top' : '' }}"
  style="{{ computed.rootStyle({ gutter }) }}">
  <slot>
  </slot>
</view>